# Day 05 - GROUP BY & HAVING

## 📌 Overview

On Day 5, I learned how to group records using the `GROUP BY` clause and filter grouped results using the `HAVING` clause. I also practiced combining aggregate functions with grouped data to generate meaningful summaries and business insights.

---

## 🎯 Topics Covered

- GROUP BY
- HAVING
- COUNT()
- SUM()
- AVG()
- MIN()
- MAX()
- WHERE vs HAVING

---

## 💻 SQL Commands Practiced

- `GROUP BY`
- `HAVING`
- `COUNT()`
- `SUM()`
- `AVG()`
- `MIN()`
- `MAX()`
- `WHERE`

---

## 🧩 LeetCode SQL Problems

| Problem | Difficulty | Status |
|---------|------------|:------:|
| 577. Employee Bonus | Easy | ✅ |
| 1075. Project Employees I | Easy | ✅ |

---

## 📂 Files Included

- `Day_05_GROUP_BY_HAVING.sql` – SQL queries demonstrating grouping and aggregation.
- `README.md` – Documentation for Day 5.
- `screenshots/` – Query execution screenshots from MySQL Workbench.
- `leetcode/` – Accepted LeetCode SQL solution screenshots.

---

## 📚 Key Learnings

- Grouped records using the `GROUP BY` clause.
- Generated summaries using aggregate functions.
- Applied `COUNT()`, `SUM()`, `AVG()`, `MIN()`, and `MAX()` with grouped data.
- Filtered grouped results using the `HAVING` clause.
- Understood the difference between `WHERE` (filters rows before grouping) and `HAVING` (filters groups after aggregation).
- Practiced writing summary reports commonly used in data analysis and business reporting.
- Strengthened SQL aggregation and grouping skills through LeetCode practice.

---

## 🛠️ Tools Used

- MySQL Server 8.0
- MySQL Workbench
- LeetCode

---

## 🚀 Next Step

**Day 06 – String Functions**

Topics to learn:

- UPPER()
- LOWER()
- LENGTH()
- CONCAT()
- SUBSTRING()
- LEFT()
- RIGHT()
- TRIM()
- REPLACE()